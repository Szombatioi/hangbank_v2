Archiving:
`archiver` library
`npm install archiver`
`npm install -D @types/archiver`


Support texts:
Maintained by system admins and text is stored in the DB, not in common.json.


Deployment log:
1. Pulled image
2. Copied .onnx file via SCP
3. `docker compose up --build`